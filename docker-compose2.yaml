version: '3.5'
services:
    culverttracker:
        build: .
        restart: always
        container_name: culvert-tracker
        environment:
          "TOKEN": "<BOT_TOKEN>"
          "CLIENT_ID": "<BOT_CLIENT_ID"
          "GUILD_ID": "<SERVER_ID>"